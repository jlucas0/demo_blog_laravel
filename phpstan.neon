includes:
    - ./vendor/nunomaduro/larastan/extension.neon

parameters:

    paths:
        - app/

    # Level 9 is the highest level
    level: 9

    # Disabled to avoid errors in model relationships methods
    checkGenericClassInNonGenericObjectType: false
